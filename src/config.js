const config = {
    API_BASE_URL: "http://localhost:3001", // Для локальной разработки
    // API_BASE_URL: "https://boss-worker-platform.onrender.com", // Для продакшна (раскомментировать при деплое)
    // API_BASE_URL: "https://boss-worker-platformjs.onrender.com"
};

export default config;
